# Yubikey/TOTP helper scripts

## Prerequisites:
   * osx
   * ruby
   * ykman (`brew install ykman`)

## Scripts

   * `otp` - CLI to list + filter TOTP sessions
   * `otp-alfred` - Script for alfred integration (returns json)
